import { SurbrillanceDirective } from './surbrillance.directive';

describe('SurbrillanceDirective', () => {
  it('should create an instance', () => {
    const directive = new SurbrillanceDirective();
    expect(directive).toBeTruthy();
  });
});
